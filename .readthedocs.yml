version: 2

conda:
  environment: docs/environment.yml

python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - tests

sphinx:
  builder: html
